This directory contains importable Python and Node.js packages in the uv workspace.
Python packages (agent-infra, agent-memory) follow the src/ layout and are editable installs.
Upstream Node.js packages (cli, core, sdk-python, etc.) are inherited from QwenLM/qwen-code -- do not modify.
