The agent-infra package provides shared infrastructure utilities used by both control-plane-daemon and agent-memory.
It is a library, not a service — no entry point, no daemon.
Python package: agent_infra — importable via the uv workspace after uv sync --all-packages.
