This directory contains shell scripts for installation and maintenance.
scripts/installation/ contains upstream Qwen Code installers — do not modify (overwritten on sync).
scripts/megalonyx/ contains Megalonyx-specific installers and utilities.
All scripts must use portable paths: derive REPO_ROOT from BASH_SOURCE, use $HOME not hardcoded usernames.
