# Git Strategy

The canonical git strategy document is at `docs/meta/git-strategy.md`.

That file covers: branch architecture, two-remote setup (`upstream` = `jdmanring/qwen-code` fork,
`origin` = `jdmanring/megalonyx-monorepo`), the two-step upstream sync process, fork sync
pipeline, contribution cherry-pick conflict patterns, LKG tags, and the no-PRs-to-QwenLM policy.
