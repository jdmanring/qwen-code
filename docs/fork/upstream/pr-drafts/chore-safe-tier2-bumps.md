# PR Draft: chore/safe-tier2-bumps → upstream/main

**Branch:** `chore/safe-tier2-bumps`
**Issue:** #15
**Status:** Needs Verification

---

## Title

`chore(deps): safe tier-2 dependency bumps`

## Description

Updates a set of stable tier-2 dependencies: `diff` 7 → 9, `read-package-up` 11 → 12, `jsdom` 26 → 29, and `vite` 5 → 8 (for webui). Adds `semver` and `msw`.

## Testing

- [ ] WebUI build and run verified
- [ ] All tests pass
