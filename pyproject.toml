[project]
name = "megacode-sovereign-stack"
version = "0.1.0"
description = "Sovereign Monorepo for Mega Code"
dependencies = []

[tool.uv]
managed = true

[tool.uv.workspace]
members = ["apps/*", "packages/*"]

# Ruff config lives in .ruff.toml (project root) — not here.
# Having config in both places causes silent conflicts; .ruff.toml wins.
