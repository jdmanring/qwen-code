registry=https://registry.npmjs.org

# pnpm workspace configuration
# strict-peer-dependencies=false: upstream packages have unresolved peer deps; don't fail install over them
strict-peer-dependencies=false
# link-workspace-packages=true: workspace packages are symlinked (default; explicit for clarity)
link-workspace-packages=true
