This directory contains runtime configuration templates for Megalonyx services.
Files here are templates only — no secrets. Live config lives in ~/.qwen/ and ~/.local/share/megalonyx/.
Every file in config/ must have a corresponding documentation mirror in docs/ (CONFIG-01 rule).
