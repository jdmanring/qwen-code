src/ layout: the control_plane_daemon Python package lives here.
This directory exists to keep the package namespace separate from project-level files (tests, pyproject.toml).
Do not put tests, configs, or scripts here — only the importable package.
